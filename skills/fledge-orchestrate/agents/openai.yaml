interface:
  display_name: "Orchestrate with Fledge"
  short_description: "Orchestrate with Fledge agents, tasks, and worktrees"
  default_prompt: "Use $fledge-orchestrate to coordinate this session through Fledge: spawn and message workers with fledge agent, track work with fledge task, and independently verify changes through completion or a concrete blocker."
policy:
  allow_implicit_invocation: false
