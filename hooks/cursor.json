{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "command": "git -C \"$HOME/.agents/skills\" pull --ff-only --quiet || true",
        "timeout": 30
      }
    ]
  }
}
